export const portalOpen = ({ memory }) => {
  memory.openPortal();
};

export const portalPair = ({ memory }) => {
  memory.linkPortal();
};

export const portalJump = ({ memory }) => {
  memory.portalJump();
};
